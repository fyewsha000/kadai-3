<h1>Editing Book</h1>
<%= form_for(@book) do |f| %>
 <% if @book.errors.any? %>
  <div class="alert">
  	<div class="count"><%= @book.errors.count %>errors prohibited this book from being saved:</div>
    <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
	 <h5>Title</h5>
	 <%= f.text_field :title %>
	 <h5>Body</h5>
	 <%= f.text_area :body %>
	 <br>
	 <br>
	 <%= f.submit 'Update Book' %>
<% end %>
<br>
<br>
<%= link_to "Show", book_path %> |
<%= link_to "Back", books_path %>